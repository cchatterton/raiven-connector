{
  "version": "0.2.1",
  "body": "Added rAIven default model selection for console requests. New installs use the service default; existing explicit choices are preserved. Clarified that key validation only checks model-list access. Improved inactive-model guidance and non-JSON HTTP errors while preserving drafts. Verified live generation with the default and qwen3.8-flash-next-nvfp4."
}
